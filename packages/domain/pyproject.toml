[project]
name = "commercevision-domain"
version = "0.1.0"
description = "Framework-independent domain boundaries"
requires-python = ">=3.13,<3.14"
dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/commercevision_domain"]
