# CommerceVision Agent

CommerceVision Agent 是面向电商视觉生产的单一编排 Agent。本文档只定义项目的统一领域语言，不记录实现方案。

## 身份

**工作区标识（Workspace ID）**:
由正则 `^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$` 定义的不透明 ASCII 租户边界标识。
系统不得修剪、折叠大小写或把非法输入规范化为另一个身份；空白、控制字符、非 ASCII 字符和
Unicode 规范化形式均为非法输入。只有完全相同的合法 Token 才表示同一工作区。
_避免使用_: 工作区名称、可规范化租户名

## 资产

**任务资产（Task Asset）**:
为一次 Workflow 上传、分析、生成或导出的输入、正文、中间结果和输出，以及承载这些内容的 Agent Checkpoint 节点数据；从 Workflow 创建起最多保存 72 小时。
_避免使用_: 临时素材、用户素材、任务数据

**基础资产（Foundation Asset）**:
经授权注册、可跨 Workflow 复用的品牌素材、Prompt 模板、LoRA、模型配置、参考素材和公开评测集；保存至管理员删除或其权利到期。
_避免使用_: 永久素材、长期任务素材、系统素材

**权利记录（Rights Record）**:
描述资产所有者、许可来源、允许用途、允许供应商、有效期和可派生条件的可审计记录。
_避免使用_: 授权备注、版权标签

**资产版本（Asset Version）**:
一次已完成上传所形成的不可变二进制内容与派生元数据快照；替换内容必须创建新版本。
_避免使用_: 文件版本、素材副本

**上传会话（Upload Session）**:
允许调用方在限定时间、对象位置、类型、大小与校验规则内向隔离区直传一个候选对象的授权。
_避免使用_: 上传链接、临时凭证

**权利到期（Rights Expiry）**:
权利记录不再允许资产继续检索、派生或发送给模型的时间边界；到期资产必须先停止使用，再完成索引和对象清理。
_避免使用_: 素材过期、自动删除时间

**删除墓碑（Deletion Tombstone）**:
资产停止使用时形成的不可变删除意图，绑定精确资产版本、删除代次与原因，并作为所有后续清理和对账的共同依据。
_避免使用_: 删除标记、清理任务、文件删除记录

## 商品理解

**商品简报（ProductBrief）**:
由商品数据、任务资产和人工修订形成的结构化商品事实与视觉约束；低置信度字段必须由人工确认。
_避免使用_: 商品分析、商品 Prompt

**品牌档案（Brand Profile）**:
由有效基础资产和品牌规则组成、可版本化引用的品牌约束集合。
_避免使用_: 品牌记忆、品牌 Prompt

## 检索

**检索引用（Retrieval Citation）**:
进入 Agent 上下文的基础资产或任务资产引用，包含资产版本、权利记录和召回理由。
_避免使用_: 搜索结果、参考图链接

**检索策略（Retrieval Policy）**:
定义硬过滤、候选召回、融合、重排和上下文裁剪规则的版本化策略。
_避免使用_: 搜索配置、向量权重

**集合升级（Collection Upgrade）**:
从 MySQL 当前权威事实构建并验证一个候选向量集合，在通过安全与质量门槛后切换检索策略指针，
并延迟退役旧集合的可恢复过程。升级期间不得原地清空或改造当前活动集合。
_避免使用_: 清库重建、Milvus 热替换、索引覆盖

## 规划与审批

**创意方案（Creative Plan）**:
针对一个 Workflow 的结构化视觉执行方案；每个版本都是不可变快照，并可追溯到确认的商品简报、
品牌档案、检索引用、规划上下文和 Prompt 版本。只有当前精确版本的批准事实才能授权后续执行。
_避免使用_: 生图 Prompt、创意文案、执行脚本

**规划上下文（Planning Context）**:
用于生成一个创意方案版本的有界、版本化事实集合，由已确认商品简报、已发布品牌约束、当前可用
检索引用和系统策略组成。
_避免使用_: 拼接 Prompt、聊天历史、隐藏上下文

**Prompt 修订版（Prompt Revision）**:
Prompt Registry 中不可变、可寻址的一版规划指令与输入输出契约；生产修订版不得原地修改。
_避免使用_: Prompt 字符串、Prompt 配置

**工具意图（Tool Intent）**:
Planner 提出的结构化工具操作建议，只表达目标和参数，不携带执行权限；服务端策略可以拒绝、收窄或
授权它，但不能把模型文本当作授权事实。
_避免使用_: 工具调用、执行命令、模型权限

**方案审批（Plan Approval）**:
审核人针对一个精确创意方案版本和 Workflow 版本追加的不可变决定；陈旧页面或其他方案版本的决定
不能授权执行。
_避免使用_: 审批状态、通过标记、计划确认

## 可靠执行

**持久操作（Durable Operation）**:
以工作区、操作种类、目标身份与版本、输入哈希唯一标识的一次可恢复业务执行；它保存认领、执行、重试、对账、完成、失败和取消事实，以及不可被最新错误覆盖的 Provider Request ID、执行/对账截止时间和恢复代次。相同逻辑身份始终指向同一操作，普通消息重放不会重新打开终态；管理员显式死信重放会追加审计历史，并按失败类型只增加一次执行或对账尝试。重放保留累计尝试数并把对应最大值设为“当前计数 + 1”，因此原预算中的未使用尝试不会被重新启用。
_避免使用_: 后台任务、Celery 任务、资产 Job

**死信重放（Dead-letter Replay）**:
操作员针对一个既有死信创建的新消息投递尝试；原死信保持不变，每次重放单独记录执行人、原因、时间、序号和新事件身份。公开历史按直接子死信稳定分页，可逐层完整遍历任意深度的再次失败链。
_避免使用_: 重置死信、删除后重试

## 模型执行

**Provider 端点能力版本（Provider Endpoint Capability Version）**:
对一个 Provider 端点、模型修订版、区域、协议和安全/数据/价格边界的不可变已审核声明；只有明确记录的
正向能力才能参与路由，未知或未验证能力均视为不可用。
_避免使用_: 模型配置、渠道配置、Provider 能力猜测

**模型路由决定（Model Route Decision）**:
系统依据一个精确能力请求、当前权利与预算事实、版本化路由策略和端点观测形成的不可变选择证据；它记录
首选端点及有界兼容后备，但不携带凭证，也不能授权绕过安全、权利、区域或未知结果围栏。
_避免使用_: 模型选择、渠道切换、降级配置

**生成批次（Generation Batch）**:
由一个当前有效方案审批及其已授权工具意图产生的一组候选生成意图，绑定精确 Workflow、创意方案版本、
方向、策略和保留边界；重复逻辑请求始终指向同一批次。
_避免使用_: 生图任务、生成 Job、批量请求

**候选槽位（Candidate Slot）**:
生成批次中具有稳定序号和逻辑身份的一个预期结果位置；每个槽位独立恢复、对账、取消并最终收敛为至多
一个候选图片，消息重放或 Worker 重启不会创建第二个有效结果。
_避免使用_: 图片序号、Provider 任务、重试次数

**候选图片（Candidate Image）**:
一个候选槽位成功收敛后形成的不可变图片事实，绑定受控任务资产版本、来源、方案/Prompt/上下文、模型
路由、安全决定和保留边界；Provider 临时 URL 不是候选图片身份。
_避免使用_: 生成链接、模型返回图、临时图片

**用量记录（Usage Record）**:
对一次精确 Provider 调用追加的、可去重的单位与费用证据；Provider 报告、配置价格、估算金额和最终金额
分别保存，缺失或冲突用量不得被当作零成本。
_避免使用_: 扣费日志、价格字段、Token 统计
